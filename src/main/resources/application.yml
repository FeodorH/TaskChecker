spring:
  application:
    name: my-spring-app

logging:
  level:
    com.example: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

server:
  port: 8080