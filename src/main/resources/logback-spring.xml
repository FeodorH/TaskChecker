<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Консольный аппендер (для всех логов) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Файловый аппендер для аспектов -->
    <appender name="ASPECT_FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/aspects.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Логгер для аспектов -->
    <logger name="AspectLogger" level="INFO">
        <appender-ref ref="ASPECT_FILE" />
    </logger>

    <!-- Корневой логгер (остальные логи) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>